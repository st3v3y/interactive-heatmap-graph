export type ChartTick = { value: string, label: string };

export type ChartData = {
    yValue: string,
    xValue: string,
    value: number
}

export type LineMarker = {
    xValue: string,
    dashed: boolean
}